const person = {
    name: 'Isaac',
    email: 'isaac@I.com',
    isActive: true
};

function createPerson ({name: string, isPaid: boolean}) {
}
createPerson({name: 'Abel', isPaid: false});

export {}