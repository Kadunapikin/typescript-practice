"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Tuser = [1, '5', true]; //this can be written without following the format of the type
//working with tupels
var tUser;
tUser = ['Isaac', false, 454]; //if you switch the arrangement youll get an error message
//using tupel on rgb
var rgb = [255, 255, 255];
var newUser = [244, 'i@i.com'];
//it is possible to rewrite the string or even use an array method to push a string into the tupel array
newUser[1] = 'izo@i.com';
newUser.push(13927);
console.log(newUser);
